model Appointment {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  modules     String
  date        DateTime
  uuid        String
  office      Office   @relation(fields: [officeId], references: [id])
  officeId    Int
}
